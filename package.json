{
  "name": "crewbee-project-context",
  "version": "0.1.0",
  "description": "Lightweight project context layer for Agent Coding and CrewBee integration.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "main": "./dist/opencode-plugin.mjs",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    },
    "./server": {
      "types": "./dist/src/integrations/opencode/plugin.d.ts",
      "import": "./dist/opencode-plugin.mjs"
    }
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "diagnostics": "node scripts/check-syntax.mjs && npm run build && node scripts/doctor-repo.mjs",
    "typecheck": "tsc --noEmit && npm run build && node scripts/check-types.mjs",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/*.test.js",
    "doctor": "npm run build && node dist/src/cli/main.js doctor",
    "primer": "npm run build && node dist/src/cli/main.js primer"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.2.24"
  }
}
