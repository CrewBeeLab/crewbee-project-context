# CrewBee Project Context Quickstart

Use `project_context_prepare` before broad project-context exploration. Use `project_context_search` only when the prepared context is insufficient. Use `project_context_finalize` after material changes.

The hidden Project Context Maintainer owns `.crewbee/.prjctxt/` reads and writes. Main agents should not need a scaffold file menu.
